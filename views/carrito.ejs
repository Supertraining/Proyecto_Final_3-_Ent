<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mi plantilla de Ingreso de Datos en EJS</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.0/dist/css/bootstrap.min.css"
        integrity="sha384-B0vP5xmATw1+K9KRQjQERJvTumQW0nPEzvF6L/Z6nronJ3oUOFUFpCjEUQouq2+l" crossorigin="anonymous">
</head>

<body class="container">
    <div class="mt-3">
        <div class="jumbotron d-block">
            <div class="d-flex my-2">
                <h3 class="my-0" style="text-decoration: underline
          black;">Usuario: </h3>
                <p class="my-0 mx-3 font-weight-bold text-primary" style="font-size: 24px;">
                   <%= user %>
                </p>
            </div>
            <div class="my-2">
                <h3 class="my-0" style="text-decoration: underline
          black;">productos: </h3>
              
                    <% if(carrito.productos.length > 0){ %>
                        <div class="table-responsive my-4">
                            <table class="table table-dark">
                                <tr style="color: yellow;"> <th>Nombre</th> <th>Imagen</th> <th>Descripcion</th> <th>Precio</th>  </tr>
                                <% for (const producto of carrito.productos) {%>
                                <tr>
                                    <td><%=producto.nombre%></td>
                                    <td><img style="width: 50px;height: 50px; border-radius: 20%;" src="<%= producto.foto %>"</td>
                                    <td><%=producto.descripcion%></td>
                                    <td><%=producto.precio%></td>
                                </tr>
                                <%};%>
                            </table>
                        </div>
                        <% } else{ %>
                            <p class="alert alert-danger">El Carrito esta vacío</p>
                        <% } %>
                
            </div>
            <button class="btn btn-dark mt-5" onclick="location.href='/inicio'">volver</button>

        </div>

</body>